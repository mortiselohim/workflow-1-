import 'package:flutter/material.dart';
import '../core/navigation/app_router.dart';

class OnboardingScreen extends StatelessWidget { const OnboardingScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Onboarding + Brand Intro'); }
class RoleSelectScreen extends StatelessWidget { const RoleSelectScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Role Select: Passenger / Rider'); }
class OtpLoginScreen extends StatelessWidget { const OtpLoginScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Phone OTP Login'); }
class PassengerHomeScreen extends StatelessWidget { const PassengerHomeScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Passenger Home: Map + Services Hub'); }
class RideRequestScreen extends StatelessWidget { const RideRequestScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Ride Request + Tier Selection'); }
class MatchingTrackingScreen extends StatelessWidget { const MatchingTrackingScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Matching + Live Tracking'); }
class TripReceiptScreen extends StatelessWidget { const TripReceiptScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Trip Receipt + Payment + Rating'); }
class DeliveryRequestScreen extends StatelessWidget { const DeliveryRequestScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Express Delivery Request + Tracking'); }
class ShoppingScreen extends StatelessWidget { const ShoppingScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Shopping + Chat + Tracking'); }
class FoodScreen extends StatelessWidget { const FoodScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Food Browse + Cart + Tracking'); }
class RentMotorcycleScreen extends StatelessWidget { const RentMotorcycleScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rent Motorcycle Marketplace + Checklist'); }
class SafetyCenterScreen extends StatelessWidget { const SafetyCenterScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Safety Center + SOS'); }
class PromotionsWalletScreen extends StatelessWidget { const PromotionsWalletScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Promotions + Discounts Wallet'); }
class ReferralScreen extends StatelessWidget { const ReferralScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Referral Program'); }
class TombolaProgressScreen extends StatelessWidget { const TombolaProgressScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Tombola Progress + Eligibility'); }
class SupportTicketsScreen extends StatelessWidget { const SupportTicketsScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Support Tickets + Help Center'); }
class PassengerProfileScreen extends StatelessWidget { const PassengerProfileScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Passenger Profile + Settings'); }
class RiderOnboardingScreen extends StatelessWidget { const RiderOnboardingScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rider Onboarding + Bike Setup'); }
class RiderHomeScreen extends StatelessWidget { const RiderHomeScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rider Home: Earnings + Requests'); }
class IncomingRequestScreen extends StatelessWidget { const IncomingRequestScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Incoming Request Accept Screen'); }
class RiderTripManagementScreen extends StatelessWidget { const RiderTripManagementScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Navigation + Trip Management'); }
class DeliveryProofScreen extends StatelessWidget { const DeliveryProofScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Delivery Proof'); }
class RiderWalletScreen extends StatelessWidget { const RiderWalletScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rider Wallet + Payouts'); }
class RiderRatingsRewardsScreen extends StatelessWidget { const RiderRatingsRewardsScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rider Ratings + Rewards Levels'); }
class RiderRentOutScreen extends StatelessWidget { const RiderRentOutScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rent Out Motorcycle Listing'); }
class MaintenanceRequestScreen extends StatelessWidget { const MaintenanceRequestScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Maintenance Request'); }
class IncidentReportScreen extends StatelessWidget { const IncidentReportScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Report Ride / Incident'); }
class RiderSafetyScreen extends StatelessWidget { const RiderSafetyScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rider Safety + SOS'); }
class RiderProfileDocsScreen extends StatelessWidget { const RiderProfileDocsScreen({super.key}); @override Widget build(BuildContext c)=>const GradientScaffold(title:'Rider Profile + Documents'); }
