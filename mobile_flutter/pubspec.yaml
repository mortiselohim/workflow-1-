name: motox_mobile
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  google_maps_flutter: ^2.9.0

flutter:
  uses-material-design: true
